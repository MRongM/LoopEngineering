---
phase: 01-protocol-0-3-contract
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - docs/superpowers/specs/2026-07-31-loop-engineering-0.3-autonomous-only-design.md
  - PROTOCOL.md
  - pyproject.toml
  - uv.lock
  - src/loop_engineering/__init__.py
  - src/loop_engineering/models/contract.py
  - src/loop_engineering/project.py
  - templates/contract.yaml
  - templates/project.yaml
  - schemas/loop-contract.schema.json
  - tests/factories.py
  - tests/test_contract.py
  - tests/test_project.py
  - tests/test_package.py
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-05, CORE-06, TEST-01]
user_setup: []
must_haves:
  truths:
    - "0.3.0 contracts omitted mode validates as autonomous"
    - "Any explicit collaborative mode is rejected for every accepted protocol version"
    - "Legacy 0.1.0/0.2.0 contracts require an explicit autonomous mode and retain their version identity"
    - "Contract template and generated schema expose only autonomous while matching model behavior"
  artifacts:
    - path: "docs/superpowers/specs/2026-07-31-loop-engineering-0.3-autonomous-only-design.md"
      provides: "Superseding approved 0.3 design and compatibility decisions"
      contains: "Autonomous-only"
    - path: "src/loop_engineering/models/contract.py"
      provides: "Strict version-aware Autonomous-only contract validation"
      contains: "PROTOCOL_VERSION = \"0.3.0\""
    - path: "schemas/loop-contract.schema.json"
      provides: "Deterministically generated 0.3 contract schema"
      contains: "0.3.0"
  key_links:
    - from: "templates/contract.yaml"
      to: "src/loop_engineering/models/contract.py"
      via: "template values accepted by LoopContract"
      pattern: "protocol_version: 0\\.3\\.0"
    - from: "schemas/loop-contract.schema.json"
      to: "src/loop_engineering/models/contract.py"
      via: "LoopContract.model_json_schema generation"
      pattern: "autonomous"
---

<objective>
Define and implement the version-aware Autonomous-only 0.3 contract surface through TDD.

Purpose: Remove collaborative without silently converting legacy omitted-mode contracts, while keeping model, protocol, templates, package version, project defaults, and generated Schema consistent.
Output: Superseding design, Protocol 0.3, failing-then-passing contract tests, strict models, templates, package metadata, and deterministic Schema.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-protocol-0-3-contract/01-CONTEXT.md
@PROTOCOL.md
@docs/superpowers/specs/2026-07-31-codex-autonomous-default-design.md
@src/loop_engineering/models/contract.py
@src/loop_engineering/contract.py
@src/loop_engineering/project.py
@tests/factories.py
@tests/test_contract.py
</context>

<feature>
  <name>Version-aware Autonomous-only contract validation</name>
  <files>tests/test_contract.py, tests/test_project.py, tests/test_package.py, tests/factories.py, src/loop_engineering/models/contract.py, src/loop_engineering/project.py, src/loop_engineering/__init__.py, pyproject.toml, uv.lock, templates/contract.yaml, templates/project.yaml, schemas/loop-contract.schema.json, PROTOCOL.md, docs/superpowers/specs/2026-07-31-loop-engineering-0.3-autonomous-only-design.md</files>
  <behavior>
    - `protocol_version=0.3.0` with no `mode` → `ControlMode.AUTONOMOUS`.
    - Any accepted protocol version with `mode=collaborative` → Pydantic validation failure containing `collaborative control mode is unsupported`.
    - `protocol_version=0.1.0` or `0.2.0` with no `mode` → validation failure requiring explicit `autonomous`.
    - Explicit legacy `autonomous` contracts remain valid; 0.1.0 legacy risk/final-gate behavior remains delegated to existing safety validation.
    - New project config defaults to `>=0.3,<0.4`; an existing `>=0.2,<0.3` config remains readable.
    - Generated contract Schema has protocol default `0.3.0`, singleton mode enum/default `autonomous`, and a legacy conditional requiring explicit mode.
  </behavior>
  <implementation>First write focused contract/project/package tests and run them to capture expected failures. Then set `PROTOCOL_VERSION` and package version to `0.3.0`, reduce `ControlMode` to `AUTONOMOUS`, add a before-validator that rejects collaborative and legacy omission, apply 0.2 risk disclosure validation to 0.3, update project defaults and templates, regenerate the lock/schema using repository commands, and rewrite PROTOCOL.md plus a superseding approved design. Do not modify historical design files.</implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML/JSON contract → strict Pydantic model | Untrusted contract input can request a control mode, protocol version, permissions and dangerous operations. |
| Generated template/Schema → downstream adapter | Drift could let another tool construct a contract that Core interprets differently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | E | `LoopContract` legacy parsing | mitigate | Reject legacy contracts without explicit `mode: autonomous` so historic collaborative defaults cannot become autonomous silently. |
| T-01-02 | T | templates and generated Schema | mitigate | Generate Schema from the strict model and assert singleton mode plus version-aware legacy requirement in tests. |
| T-01-03 | R | superseded design history | mitigate | Add a new dated design with explicit precedence; retain old design unchanged for auditability. |
</threat_model>

<verification>
- [ ] Focused contract/project/package tests fail before production changes for the expected missing 0.3 behavior.
- [ ] `python -m pytest tests/test_contract.py tests/test_project.py tests/test_package.py -q` passes after implementation.
- [ ] `loop-engineering export-schemas --output schemas` or the repository-equivalent argv command deterministically reproduces `schemas/loop-contract.schema.json`.
- [ ] `git diff --check` passes.
</verification>

<success_criteria>
- RED evidence proves tests detect the old collaborative default and missing 0.3 support.
- GREEN evidence proves all focused tests pass without weakening existing safety assertions.
- Protocol, model, template, package metadata, project default and generated Schema describe the same 0.3 behavior.
- Historical 0.1/0.2 Autonomous contracts are explicit and readable; ambiguous or collaborative legacy contracts fail closed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-0-3-contract/01-01-SUMMARY.md`.
</output>

