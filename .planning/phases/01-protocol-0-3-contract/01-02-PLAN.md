---
phase: 01-protocol-0-3-contract
plan: "02"
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/loop_engineering/models/run.py
  - src/loop_engineering/ledger.py
  - src/loop_engineering/policy.py
  - tests/test_ledger.py
  - tests/test_policy.py
  - tests/test_evidence.py
  - tests/e2e/test_risk_gates.py
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03]
user_setup: []
must_haves:
  truths:
    - "0.3 approvals bind protocol version, contract version, canonical SHA-256 and every disclosed risk ID"
    - "0.2 and 0.3 autonomous contracts use the same exact risk authorization gates"
    - "0.1 autonomous contracts preserve legacy dangerous-action and high-risk final-acceptance gates"
    - "Forbidden Git operations remain denied for all supported protocols"
  artifacts:
    - path: "src/loop_engineering/models/run.py"
      provides: "Strict 0.2/0.3 ContractAuthorization model"
      contains: "0.3.0"
    - path: "src/loop_engineering/ledger.py"
      provides: "Bound approval persistence and protocol upgrade/downgrade checks"
      contains: "current_contract_authorization"
    - path: "src/loop_engineering/policy.py"
      provides: "Exact risk and permanent-deny policy for 0.2/0.3 autonomous contracts"
      contains: "_uses_autonomous_risk_grant"
  key_links:
    - from: "src/loop_engineering/ledger.py"
      to: "src/loop_engineering/models/run.py"
      via: "ContractAuthorization validation"
      pattern: "ContractAuthorization\\.model_validate"
    - from: "src/loop_engineering/policy.py"
      to: "src/loop_engineering/contract.py"
      via: "authorization fingerprint equality"
      pattern: "contract_fingerprint"
---

<objective>
Extend bound authorization and safety gates from 0.2 to 0.3 without changing legacy 0.1 behavior.

Purpose: Prove Autonomous-only is a mode simplification, not a permission expansion or a weakened completion standard.
Output: Failing-then-passing ledger/policy/evidence tests and minimal 0.3 authorization implementation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-protocol-0-3-contract/01-CONTEXT.md
@.planning/phases/01-protocol-0-3-contract/01-01-SUMMARY.md
@PROTOCOL.md
@src/loop_engineering/models/run.py
@src/loop_engineering/ledger.py
@src/loop_engineering/policy.py
@src/loop_engineering/evidence.py
@tests/test_ledger.py
@tests/test_policy.py
@tests/e2e/test_risk_gates.py
</context>

<feature>
  <name>0.3 bound authorization and safety compatibility</name>
  <files>tests/test_ledger.py, tests/test_policy.py, tests/test_evidence.py, tests/e2e/test_risk_gates.py, src/loop_engineering/models/run.py, src/loop_engineering/ledger.py, src/loop_engineering/policy.py</files>
  <behavior>
    - An approved 0.3 contract records and reloads authorization bound to `0.3.0`, current contract version, canonical fingerprint and complete risk IDs.
    - Missing, stale or incomplete 0.3 authorization pauses scoped writes and exact risky operations at contract approval/revision gates.
    - 0.2 explicit Autonomous contracts continue using identical bound authorization behavior.
    - 0.1 explicit Autonomous contracts do not acquire 0.2/0.3 grants and retain legacy fresh dangerous-action/high-risk final-acceptance behavior.
    - Protocol replacement may upgrade 0.1→0.2/0.3 or 0.2→0.3 with a new contract version and approval, but rejects every downgrade.
    - Force-push, history rewrite, reset-hard-equivalent policy operations, automatic merge and deployment remain denied.
  </behavior>
  <implementation>First add or replace ledger, policy and end-to-end tests so 0.3 authorization fails on the current 0.2-only implementation while legacy assertions remain. Then extend `ContractAuthorization`, `RunStore` approval/summary/replacement logic and `GatePolicy._uses_autonomous_risk_grant` to the explicit set `{0.2.0, 0.3.0}`. Replace pair-specific downgrade logic with a small ordered-version comparison. Keep `DoneEvaluator` driven by explicit human gates so legacy 0.1 high-risk final acceptance remains intact.</implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Approval event → executable authority | Ledger payload decides whether a state-changing action may run without another prompt. |
| Persisted contract → resumed Run | Tampering or stale approvals could grant authority to a different objective, risk or target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | E | `RunStore.current_contract_authorization` | mitigate | Require exact protocol/version/hash/risk-ID equality before returning authorization for 0.2 or 0.3. |
| T-01-05 | T | contract replacement | mitigate | Enforce monotonic protocol versions and incremented contract versions; record a fresh bound revision approval. |
| T-01-06 | R | permanent deny policy | mitigate | Retain parameterized tests proving force-push, history rewrite, merge and deploy always return DENY. |
</threat_model>

<verification>
- [ ] Focused ledger/policy/evidence/e2e tests fail before production changes for missing 0.3 authorization support.
- [ ] `python -m pytest tests/test_ledger.py tests/test_policy.py tests/test_evidence.py tests/e2e/test_risk_gates.py -q` passes after implementation.
- [ ] `python -m pytest tests/test_contract.py tests/test_project.py tests/test_package.py tests/test_ledger.py tests/test_policy.py tests/test_evidence.py tests/e2e/test_risk_gates.py -q` passes as a Phase 1 regression set.
- [ ] `git diff --check` passes.
</verification>

<success_criteria>
- RED and GREEN evidence is preserved for the 0.3 authorization behavior.
- Exact grants, stale-approval rejection, legacy gates and permanent denies all remain covered by behavioral tests.
- No mode-specific collaborative branch remains reachable in Core models or policy.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-0-3-contract/01-02-SUMMARY.md`.
</output>

