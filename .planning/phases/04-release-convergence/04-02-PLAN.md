---
phase: 04-release-convergence
plan: "02"
type: verification
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: true
requirements: [TEST-02]
user_setup: []
must_haves:
  truths:
    - "D-05: Fresh evidence proves protocol, CLI, lifecycle, Adapter, tests, static analysis, build and diff integrity"
    - "D-06: Release verification performs no global install/uninstall and writes generated proof only to private temporary storage"
    - "The built distribution remains loop-engineering 0.3.0 and exposes only the loop-engine console script"
  artifacts:
    - path: ".planning/phases/04-release-convergence/04-02-SUMMARY.md"
      provides: "Fresh cross-layer release evidence and exact command results"
      contains: "Release Evidence"
  key_links:
    - from: "pyproject.toml"
      to: "built wheel metadata"
      via: "uv build in a private temporary output directory"
      pattern: "loop-engine"
    - from: "schemas/loop-contract.schema.json"
      to: "Pydantic models"
      via: "temporary deterministic schema export and byte comparison"
      pattern: "autonomous"
---

<objective>
Prove the complete 0.3.0 release surface with fresh, reproducible, cross-layer evidence.

Purpose: A passing implementation is not release-ready until the installed command contract, lifecycle suite, generated schemas, build metadata and complete regression set agree.
Output: A verification-only summary recording exact fresh results without global lifecycle mutations or repository build artifacts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-release-convergence/04-CONTEXT.md
@.planning/phases/04-release-convergence/04-01-SUMMARY.md
@PROTOCOL.md
@pyproject.toml
@schemas/loop-contract.schema.json
@adapters/codex/SKILL.md
@adapters/codex/scripts/manage.py
@tests
</context>

<task>
  <name>Collect fresh release evidence</name>
  <files>.planning/phases/04-release-convergence/04-02-SUMMARY.md</files>
  <action>Run focused protocol/CLI/lifecycle/Adapter suites, then the complete repository suite and Ruff. Invoke the local `loop-engine --version` and `--help` entry point, verify all ten command groups and absence of legacy scripts. Export schemas to a newly allocated `/private/tmp` directory and compare them with tracked schemas. Build sdist/wheel into a separate newly allocated `/private/tmp` directory, inspect wheel metadata and entry points for exact distribution/version/script identity, then run active-document residue scans, GSD schema-drift and `git diff --check`. Do not run managed global install/update/uninstall.</action>
  <verify>
    - `uv run python -m pytest -q` passes with zero skipped/failed release requirements.
    - `uv run ruff check .` passes.
    - `uv run loop-engine --version` reports `0.3.0`; help exposes project, contract, schema, run, evidence, budget, completion, gate, scope and git.
    - Lifecycle and Adapter focused suites pass.
    - Temporary schema export matches tracked schemas.
    - Temporary build succeeds and wheel metadata contains distribution `loop-engineering` 0.3.0 with only `loop-engine = loop_engineering.cli:main`.
    - Active-doc legacy-command scan, schema-drift and `git diff --check` are clean.
  </verify>
  <done>All release layers agree on Autonomous-only Protocol 0.3.0 and the unique `loop-engine` executable, with fresh evidence recorded in the summary.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source metadata → built artifact | A successful source test can still hide incorrect packaged entry points. |
| Generated model → tracked schema | Stale schemas can misrepresent accepted contracts. |
| Old evidence → release claim | Previous green results do not prove the current fingerprint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T | wheel metadata | mitigate | Inspect freshly built wheel metadata and entry points. |
| T-04-05 | R | release evidence | mitigate | Record fresh command results after the final source changes. |
| T-04-06 | I/D | lifecycle environment | avoid | Use isolated tests; do not mutate global tool installations. |
</threat_model>

<verification>
- [ ] Every listed fresh command exits zero or produces the explicitly expected negative-scan result.
- [ ] Build and schema outputs exist only under newly allocated `/private/tmp` directories.
- [ ] No source, test, gate or schema is weakened to obtain release GREEN.
</verification>

<success_criteria>
- TEST-02 has direct fresh evidence for every named layer.
- Built artifacts prove the same identities as source metadata and lifecycle code.
- Repository diff remains structurally clean and adds no generated runtime/build state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-release-convergence/04-02-SUMMARY.md`.
</output>
