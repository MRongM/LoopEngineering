---
phase: 03-autonomous-codex-skill
plan: "02"
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/test_adapter_contract.py
  - adapters/codex/SKILL.md
autonomous: true
requirements: [AUTO-02, AUTO-03, AUTO-04]
user_setup: []
must_haves:
  truths:
    - "D-04: One complete contract approval starts a continuous design-plan-execute-verify-check-decide loop"
    - "D-05: Tests, command results, fresh evidence and Checker verdicts determine the next smallest action"
    - "D-06: The Skill pauses only at enumerated contract, authority, reconciliation, external, cancellation or budget boundaries"
  artifacts:
    - path: "adapters/codex/SKILL.md"
      provides: "Evidence-driven autonomous decision and pause protocol"
      contains: "## Autonomous decision loop"
    - path: "tests/test_adapter_contract.py"
      provides: "Decision matrix and hard-boundary contract assertions"
      contains: "test_codex_skill_runs_the_autonomous_decision_loop"
  key_links:
    - from: "adapters/codex/SKILL.md"
      to: "PROTOCOL.md"
      via: "deterministic decision matrix"
      pattern: "smallest scoped action"
    - from: "adapters/codex/SKILL.md"
      to: "tests/test_adapter_contract.py"
      via: "ACCEPT/REVISE/BLOCK handling"
      pattern: "Checker"
---

<objective>
Make the post-approval Codex Skill continuously choose, verify and correct the next smallest action until a real terminal state or exact hard boundary.

Purpose: Autonomous must mean evidence-driven execution inside the approved contract, not merely a default field value.
Output: Failing-then-passing decision-loop tests and an explicit Maker/Checker/decision protocol with exhaustive pause boundaries.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-autonomous-codex-skill/03-CONTEXT.md
@.planning/phases/03-autonomous-codex-skill/03-PATTERNS.md
@.planning/phases/03-autonomous-codex-skill/03-01-SUMMARY.md
@PROTOCOL.md
@adapters/codex/SKILL.md
@tests/test_adapter_contract.py
</context>

<feature>
  <name>Evidence-driven autonomous decision loop</name>
  <files>tests/test_adapter_contract.py, adapters/codex/SKILL.md</files>
  <read_first>
    - PROTOCOL.md
    - .planning/phases/03-autonomous-codex-skill/03-CONTEXT.md
    - .planning/phases/03-autonomous-codex-skill/03-01-SUMMARY.md
    - adapters/codex/SKILL.md
    - tests/test_adapter_contract.py
  </read_first>
  <behavior>
    - After one current bound contract approval, proceed through designing, planning, executing, verifying, checking and deciding without another ordinary confirmation.
    - Each iteration selects exactly one unmet acceptance criterion and one smallest scoped action, checks budget and Gate, records intent/result, captures fresh evidence and invokes independent Checker for medium/high risk.
    - Fresh progress continues to the next criterion; a new test/command failure returns to diagnosis; Checker REVISE returns to planning/execution; Checker BLOCK stops; ACCEPT plus all completion facts runs authoritative completion evaluation.
    - Two no-progress iterations force diagnosis; the same failed strategy is attempted at most once; no prose, stale evidence or missing Checker can produce DONE.
    - Pause only for a complete contract revision, missing/stale approval, unresolved or ambiguous Goal/Run/intent reconciliation, external authentication/platform gate, missing necessary authority/input/Checker, user cancellation, or authoritative budget/terminal state.
    - Permanent deny operations remain deny outcomes and are never converted into confirmation prompts.
  </behavior>
  <implementation>First add a focused Adapter contract test for the full stage sequence, smallest-action invariant, evidence-driven outcome table, retry/no-progress rules and exhaustive pause list. Capture RED. Then consolidate the existing Maker, Checker, control-mode and approval sections into one Autonomous decision loop plus a precise hard-boundaries section, retaining authoritative completion and permanent-deny behavior.</implementation>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_adapter_contract.py -q -k 'autonomous_decision_loop or pauses_only_at_hard_boundaries or completion_requires_fresh_evidence'` exits 0.
    - Skill contains the ordered stage names `designing`, `planning`, `executing`, `verifying`, `checking`, `deciding` and the phrase `one unmet acceptance criterion`.
    - Skill contains explicit decision outcomes for progress, diagnosis, Checker `REVISE`, Checker `BLOCK`, authoritative completion and budget exhaustion.
    - Skill enumerates contract revision, stale approval, reconciliation, external/authentication, missing authority/input/Checker, cancellation and budget boundaries while stating risk level alone is not a pause.
  </acceptance_criteria>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model decision → external mutation | Every mutation must remain authorized, intentional and observable. |
| Command/test output → completion | Stale or fabricated evidence must not satisfy acceptance. |
| Checker result → next state | Independent verdict must be preserved and handled without Maker override. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | action execution | mitigate | Gate check plus adjacent intent/result around every external state change. |
| T-03-05 | R | autonomous decision | mitigate | Persist real command/test/evidence and deterministic decision outcome each iteration. |
| T-03-06 | D | retry loop | mitigate | Same-strategy and no-progress limits return to diagnosis or terminal budget state. |
| T-03-07 | E | false DONE | mitigate | Fresh evidence, current fingerprint, required Checker ACCEPT and authoritative completion command. |
</threat_model>

<verification>
- [ ] Focused decision-loop tests fail before Skill changes for missing stage/matrix/boundary language.
- [ ] Focused tests pass after minimal Skill consolidation.
- [ ] All Adapter contract tests, the full repository suite and Ruff pass.
- [ ] No collaborative or legacy runtime CLI command remains in the Skill body.
- [ ] `git diff --check` passes.
</verification>

<success_criteria>
- One approval leads to continuous bounded autonomous execution.
- Every next step is justified by current persisted evidence or an explicit hard stop.
- The Skill cannot ask for routine confirmations, loop without progress or self-declare DONE.
</success_criteria>

<output>
After completion, create `.planning/phases/03-autonomous-codex-skill/03-02-SUMMARY.md`.
</output>
