---
phase: 03-autonomous-codex-skill
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_adapter_contract.py
  - adapters/codex/SKILL.md
autonomous: true
requirements: [CORE-04, AUTO-01, AUTO-05]
user_setup: []
must_haves:
  truths:
    - "D-01: Every new Codex Loop task is a Protocol 0.3 Autonomous contract without a mode choice"
    - "D-02: The Adapter contains no collaborative selection, display, downgrade or dedicated path"
    - "D-03: Every continuation revalidates the canonical Goal/Run, ledger, current authorization, budget and pending intents"
  artifacts:
    - path: "adapters/codex/SKILL.md"
      provides: "Autonomous-only admission and deterministic Goal/Run resume protocol"
      contains: "protocol_version: 0.3.0"
    - path: "tests/test_adapter_contract.py"
      provides: "Positive and negative Adapter contract assertions"
      contains: "test_codex_skill_is_autonomous_only"
  key_links:
    - from: "adapters/codex/SKILL.md"
      to: "PROTOCOL.md"
      via: "0.3 contract and authorization semantics"
      pattern: "Compatible Core: >=0.3,<0.4"
    - from: "adapters/codex/SKILL.md"
      to: "src/loop_engineering/ledger.py"
      via: "canonical marker plus events/status/authorization/budget/pending-intent revalidation"
      pattern: "contract_sha256"
---

<objective>
Remove the remaining control-mode surface from the Codex Skill and make Goal/Run continuation explicitly revalidate all durable authority.

Purpose: A new or resumed `$loop-engine` task must never depend on a mode prompt, conversation memory or newest-Run guessing.
Output: Failing-then-passing Adapter contract tests and an Autonomous-only admission/resume instruction contract using the `loop-engine` runtime command.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-autonomous-codex-skill/03-CONTEXT.md
@.planning/phases/03-autonomous-codex-skill/03-PATTERNS.md
@PROTOCOL.md
@docs/superpowers/specs/2026-07-31-loop-engineering-0.3-autonomous-only-design.md
@adapters/codex/SKILL.md
@tests/test_adapter_contract.py
</context>

<feature>
  <name>Autonomous-only admission and durable continuation</name>
  <files>tests/test_adapter_contract.py, adapters/codex/SKILL.md</files>
  <read_first>
    - PROTOCOL.md
    - docs/superpowers/specs/2026-07-31-loop-engineering-0.3-autonomous-only-design.md
    - .planning/phases/03-autonomous-codex-skill/03-CONTEXT.md
    - adapters/codex/SKILL.md
    - tests/test_adapter_contract.py
  </read_first>
  <behavior>
    - Adapter body contains no `collaborative` token and no mode selection, downgrade, upgrade or mode-specific final-acceptance path.
    - Intake always creates Protocol 0.3 with `mode: autonomous`, while explicit incompatible mode input is rejected instead of interpreted.
    - Every runtime Core command in the Skill uses `loop-engine`; `loop-engineering` remains only a distribution/repository/checkout identity.
    - Resume verifies the `$loop-engine goal-bridge/v1` objective, loop ID and absolute Run path, then runs events/status, reconciles pending intents, validates the current protocol/version/hash/risk binding and checks budget before another action.
    - Missing, stale, ambiguous or unrelated bindings pause without scanning `.loop-runs/` for a newest candidate.
  </behavior>
  <implementation>First replace the existing mode-default tests with Autonomous-only positive/negative assertions and strengthen the continuation test with exact 0.3 authorization and `loop-engine` command requirements. Capture RED against the old Skill. Then minimally rewrite admission, resume, compatibility and approval-reference sections while preserving lifecycle and Goal intent/result safety.</implementation>
  <acceptance_criteria>
    - `uv run python -m pytest tests/test_adapter_contract.py -q -k 'autonomous_only or defaults_every_new_task or goal_bound_continuation'` exits 0.
    - `tests/test_adapter_contract.py` asserts `collaborative` is absent from the Skill body.
    - `tests/test_adapter_contract.py` asserts `protocol_version: 0.3.0`, `mode: autonomous`, `contract_sha256`, `accepted_risk_ids`, `loop-engine run events`, `loop-engine run status` and `loop-engine budget check` are present.
    - No assertion accepts a runtime `loop-engineering` command or a mode-selection phrase.
  </acceptance_criteria>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User message → task admission | An unrelated or incompatible message must not create or adopt a Run. |
| Native Goal → Loop Run | Goal metadata is only a pointer and must not be treated as approval. |
| Conversation memory → persisted authority | Model memory cannot substitute for ledger and contract binding state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S | implicit task selection | mitigate | Require explicit start or one canonical verified Goal/Run binding; forbid newest-Run scanning. |
| T-03-02 | E | stale Goal/approval | mitigate | Revalidate protocol, contract version/hash, accepted risks, status, budget and pending intents before action. |
| T-03-03 | R | ambiguous mode history | mitigate | New tasks are fixed Autonomous 0.3 and the Adapter exposes no alternative mode path. |
</threat_model>

<verification>
- [ ] Focused Adapter tests fail against the old mode branches and old command name.
- [ ] Focused tests pass after the Skill rewrite.
- [ ] Full Adapter contract tests and Ruff pass.
- [ ] `git diff --check` passes.
</verification>

<success_criteria>
- The Skill admits only Autonomous 0.3 tasks without asking a mode question.
- Resume uses durable Goal/Run/ledger facts before any next mutation.
- No collaborative Adapter behavior remains.
</success_criteria>

<output>
After completion, create `.planning/phases/03-autonomous-codex-skill/03-01-SUMMARY.md`.
</output>
