---
phase: 02-cli-and-lifecycle
plan: "02"
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - adapters/codex/scripts/manage.py
  - adapters/codex/SKILL.md
  - tests/test_codex_installer.py
  - tests/test_adapter_contract.py
autonomous: true
requirements: [LIFE-01]
user_setup: []
must_haves:
  truths:
    - "Lifecycle install and update manage distribution loop-engineering but verify executable loop-engine"
    - "Lifecycle uninstall addresses distribution loop-engineering and verifies loop-engine removal"
    - "Lifecycle validation rejects package metadata that registers either legacy CLI alias"
    - "All lifecycle subprocesses remain argv-based with shell disabled and existing deletion gates intact"
  artifacts:
    - path: "adapters/codex/scripts/manage.py"
      provides: "Distribution-aware and executable-aware lifecycle operations"
      contains: "CLI_NAME = \"loop-engine\""
    - path: "tests/test_codex_installer.py"
      provides: "Install/update/uninstall command and verification matrix"
      contains: "loop-engine"
  key_links:
    - from: "adapters/codex/scripts/manage.py"
      to: "pyproject.toml"
      via: "validated project name, version and scripts metadata"
      pattern: "project_data"
    - from: "adapters/codex/scripts/manage.py"
      to: "loop-engine executable"
      via: "post-install version probe and post-uninstall absence check"
      pattern: "_verify_.*cli"
---

<objective>
Carry the unique CLI name through the safe Codex install/update/uninstall lifecycle.

Purpose: Keep the product and Python distribution identity stable while proving the correct executable is installed and removed.
Output: Failing-then-passing lifecycle tests, explicit identity constants, 0.3 checkout markers and verified uv operations.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cli-and-lifecycle/02-CONTEXT.md
@.planning/phases/02-cli-and-lifecycle/02-01-SUMMARY.md
@adapters/codex/scripts/manage.py
@adapters/codex/SKILL.md
@tests/test_codex_installer.py
@pyproject.toml
</context>

<feature>
  <name>Distribution/executable lifecycle separation</name>
  <files>tests/test_codex_installer.py, tests/test_adapter_contract.py, adapters/codex/scripts/manage.py, adapters/codex/SKILL.md</files>
  <behavior>
    - Checkout validation requires product package `loop-engineering`, version 0.3.0, and exactly one script named `loop-engine`.
    - Install/update invoke `uv tool install [--reinstall] <repository>`, then resolve and execute `loop-engine --version` with shell disabled.
    - A missing/wrong-version executable or discoverable `loop-engineering`/`loop-agent` alias fails closed and retains the checkout.
    - Uninstall invokes `uv tool uninstall loop-engineering`; success or exact already-absent output is accepted only when `loop-engine` is no longer discoverable before checkout removal.
    - Existing dirty-worktree, Git-boundary, explicit `--yes`, outside-checkout and revalidation protections remain unchanged.
  </behavior>
  <implementation>First update lifecycle fixtures/assertions to Protocol 0.3, the new compatibility marker, separated names, exact script metadata, post-install probe and post-uninstall absence. Capture RED. Then introduce focused constants and verification helpers in `manage.py`, call them only after successful uv operations, and update lifecycle-specific Skill marker/version commands. Reuse `_run` for every process and never invoke a shell.</implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checkout metadata → installed tool | Tampered package metadata could register an old or unexpected executable. |
| uv result → checkout deletion | A failed or partial uninstall must not delete the recovery checkout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S/E | project scripts metadata | mitigate | Require exact distribution/version/script mapping before uv install. |
| T-02-02 | T | install/update result | mitigate | Probe exact executable/version and reject legacy aliases after uv success. |
| T-02-03 | D | uninstall recovery | mitigate | Confirm new executable absence before recursively removing the validated checkout. |
</threat_model>

<verification>
- [ ] Focused lifecycle tests fail before production changes for old markers and missing executable verification.
- [ ] `uv run python -m pytest tests/test_codex_installer.py tests/test_adapter_contract.py -q` passes.
- [ ] `uv run ruff check adapters/codex/scripts tests/test_codex_installer.py tests/test_adapter_contract.py` passes.
- [ ] Full tests and `git diff --check` pass.
</verification>

<success_criteria>
- Lifecycle uses the distribution name only where uv requires it and the CLI name everywhere executable behavior is verified.
- Old aliases are neither registered nor accepted as successful installation state.
- Destructive checkout removal remains behind all existing gates plus executable-absence verification.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-and-lifecycle/02-02-SUMMARY.md`.
</output>
