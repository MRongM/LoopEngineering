---
phase: 02-cli-and-lifecycle
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/loop_engineering/cli.py
  - tests/test_cli.py
  - tests/test_package.py
autonomous: true
requirements: [CLI-01, CLI-02]
user_setup: []
must_haves:
  truths:
    - "The installed distribution exposes loop-engine as its only console script"
    - "loop-engine retains every existing command group and reports version 0.3.0"
    - "Neither loop-engineering nor loop-agent is registered as a console-script alias"
  artifacts:
    - path: "pyproject.toml"
      provides: "Unique loop-engine console-script metadata"
      contains: "loop-engine = \"loop_engineering.cli:main\""
    - path: "src/loop_engineering/cli.py"
      provides: "CLI parser branded with the executable name"
      contains: "prog=\"loop-engine\""
  key_links:
    - from: "pyproject.toml"
      to: "src/loop_engineering/cli.py"
      via: "project.scripts entry point"
      pattern: "loop_engineering\.cli:main"
---

<objective>
Switch the Python console-script surface to the single `loop-engine` executable through TDD.

Purpose: Deliver the requested Shell entry name without changing the distribution/import names or duplicating command implementations.
Output: Failing-then-passing metadata/parser tests and one unique console-script declaration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cli-and-lifecycle/02-CONTEXT.md
@pyproject.toml
@src/loop_engineering/cli.py
@tests/test_cli.py
@tests/test_package.py
</context>

<feature>
  <name>Unique loop-engine console entry</name>
  <files>tests/test_cli.py, tests/test_package.py, pyproject.toml, src/loop_engineering/cli.py</files>
  <behavior>
    - Project metadata contains exactly one console script: `loop-engine = loop_engineering.cli:main`.
    - Metadata contains no `loop-engineering` or `loop-agent` script key.
    - Parser help/usage identifies itself as `loop-engine`.
    - Existing project, contract, schema, run, evidence, budget, completion, gate, scope and git groups remain registered.
    - `loop-engine --version` reports 0.3.0 after locked environment refresh.
  </behavior>
  <implementation>Write metadata and parser-surface tests first and capture failure against the old script name. Replace only the project script key and argparse `prog`, refresh the editable environment through uv, then run focused CLI/package tests. Do not add wrappers or aliases.</implementation>
</feature>

<verification>
- [ ] Focused CLI metadata tests fail before production changes for the old entry name.
- [ ] `uv run python -m pytest tests/test_cli.py tests/test_package.py -q` passes.
- [ ] `uv run loop-engine --version` prints `0.3.0`.
- [ ] The refreshed environment does not contain project-installed `loop-engineering` or `loop-agent` scripts.
- [ ] `git diff --check` passes.
</verification>

<success_criteria>
- RED/GREEN evidence proves the unique executable metadata and parser name.
- All existing command groups remain implemented by the same `main` function.
- No compatibility alias is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-and-lifecycle/02-01-SUMMARY.md`.
</output>
