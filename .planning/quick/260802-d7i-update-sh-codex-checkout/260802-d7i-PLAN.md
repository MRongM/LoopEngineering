---
quick_id: 260802-d7i
status: complete
mode: inline
description: Add update.sh for the managed Codex checkout
must_haves:
  truths:
    - The root update.sh delegates to the existing Codex lifecycle manager instead of duplicating update policy.
    - The wrapper works from any current directory and preserves CODEX_HOME paths containing spaces.
    - The wrapper propagates lifecycle-manager failures without deleting, resetting, or repairing the checkout.
    - Focused and full verification pass with the existing manager safety gates unchanged.
  artifacts:
    - update.sh
    - tests/test_update_script.py
    - README.md
  key_links:
    - update.sh resolves adapters/codex/scripts/manage.py from its own checkout.
    - update.sh passes update and --codex-home as separate quoted argv elements.
    - README documents the wrapper alongside the existing Unix and PowerShell flows.
---

# Quick Task 260802-d7i: Managed Update Script

> Execute inline. Do not create implementation subagents, branches, worktrees or Git commits.

**Goal:** Add an executable root `update.sh` that updates the canonical Codex managed checkout
through the existing lifecycle manager.

**Canonical design:** `docs/superpowers/specs/2026-08-02-managed-update-script-design.md`

**Canonical implementation plan:** `docs/superpowers/plans/2026-08-02-managed-update-script.md`

## Task 1: Establish RED evidence and implement the wrapper

- [x] Add real subprocess tests for explicit/default `CODEX_HOME`, arbitrary current directories,
  paths containing spaces, exact manager argv, failure propagation, executable mode, and POSIX syntax.
- [x] Run the focused tests before production code and retain the expected missing-script failures.
- [x] Add the minimal executable POSIX wrapper and run focused GREEN verification.

## Task 2: Document and verify the update entrypoint

- [x] Document `./update.sh` beside the existing shell update flow.
- [x] Run focused wrapper and lifecycle-manager regression tests.
- [x] Run full pytest, Ruff, POSIX syntax, whitespace, status, and diff checks.
- [x] Write the GSD summary and update STATE without a commit claim.

## Constraints

- Delegate all Git, origin, branch, fast-forward, marker, reinstall, and version validation to
  `adapters/codex/scripts/manage.py`.
- Keep Core tool-independent and do not add dependencies or unrelated refactors.
- Do not weaken tests, Gates, schemas, or checkout-preservation behavior.
- Do not branch, commit, push, create a PR, merge, deploy, reset, or rewrite history.
